import 'package:flutter/material.dart';

import '../../xml/search/xmlsearchbus.dart';

class SearchBus extends StatelessWidget {
  const SearchBus({super.key});

  @override
  Widget build(BuildContext context) {
    return const XmlSearchBus();
  }
}