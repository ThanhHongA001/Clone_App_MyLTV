import 'package:flutter/material.dart';

import '../../xml/search/xmlsearchtimetable.dart';

class SearchTimetable extends StatelessWidget {
  const SearchTimetable({super.key});

  @override
  Widget build(BuildContext context) {
    return const XmlSearchTimetable();
  }
}